package Game;

class Constants {
    static int windowWidth = 800;
    static int windowHeight = 500;

    static int backgroundColor = 125;

    static int slideBlockWidth = 100;
    static int slideBlockkHeight = 20;
    static int slideBlockX = windowWidth / 2;
    static int slideBlockY = windowHeight - slideBlockkHeight;
    static int slideBlockColor = 250;

    static int ballWidth = 20;
    static int ballHeight = 20;
    static int ballX = windowWidth / 2 + slideBlockWidth/2;
    static int ballY = windowHeight - (slideBlockkHeight + ballWidth/2);

    static int ballColor = 255;

    static int blockX = 0;
    static int blockY = 0;
    static int blockWidth = 80;
    static int blockHeight = 10;
    static int blockSpace = 5;
    static int blockColor = 255;
    static int blockInitX = 0;
}
