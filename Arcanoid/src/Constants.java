class Constants {
    static final int PADDLE_WIDTH = 100;
    static final int PADDLE_HEIGHT = 20;

    static final int BALL_SPEED = 4;
    static final int BALL_SIZE = 15;
    static final int BALL_RADIUS = BALL_SIZE / 2;

    static final int WINDOW_WIDTH = 600;
    static final int WINDOW_HEIGHT = 400;
    static final int BRICK_WIDTH = WINDOW_WIDTH / 10;
    static final int BRICK_HEIGHT = WINDOW_HEIGHT / 20;
}
